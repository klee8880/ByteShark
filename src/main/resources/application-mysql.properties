# database init, supports mysql too
database=mysql
spring.datasource.url=jdbc:mysql://localhost/byteShark?createDatabaseIfNotExist=true
spring.datasource.username=byteShark
spring.datasource.password=byteShark
# SQL is written to be idempotent so this is safe
spring.datasource.initialization-mode=always
